eth:
  outputs:
    ci:
      account: MJLBCSA-FJ82913
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: Swapnil
      warehouse: compute_wh
  target: ci
#comments